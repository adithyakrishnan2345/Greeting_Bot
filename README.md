Add the python script to the startup folder in Windows. 
For finding the startup folder, press Windows+R , type shell:startup.
In the startup folder, click New -> shortcut, add the location of the python script
